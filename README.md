My Dotfiles. They're not perfect, but they're mine.

Clone using `--recursive` to install with bundled vim plugins.

Then add a symlink to the bundled plugins and colours using `ln -s ~/path/to/Dotfiles/.vim ~/.vim`.
